# Backend environment template
# Copy to .env and fill with real credentials before running the backend.

BGH_EMAIL=
BGH_PASSWORD=
# Optional: override default request timeout (milliseconds)
BGH_TIMEOUT_MS=
