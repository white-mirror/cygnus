{
  "name": "cygnus",
  "version": "1.0.0",
  "description": "Monorepo de Cygnus: plataforma para administrar dispositivos con un backend Express y un frontend React.",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "format": "prettier --write .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "concurrently": "^9.1.0",
    "prettier": "^3.4.1"
  }
}
